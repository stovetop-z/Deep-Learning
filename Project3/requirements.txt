# Project 3: CNNs in PyTorch - Requirements
# Install PyTorch separately based on your system (see README.md)

# Core dependencies
numpy>=1.21.0
matplotlib>=3.4.0
seaborn>=0.11.0
pillow>=8.3.0
tqdm>=4.62.0
scikit-learn>=0.24.0

# Jupyter and notebook tools
jupyter>=1.0.0
notebook>=6.4.0
ipykernel>=6.4.0
nbconvert>=6.1.0

# For PDF export (optional, but recommended)
# On macOS/Linux, you may also need to install pandoc and texlive:
# brew install pandoc
# brew install --cask mactex
# OR
# sudo apt-get install pandoc texlive-xetex texlive-fonts-recommended texlive-plain-generic

# Additional useful tools
ipywidgets>=7.6.0
pandas>=1.3.0
